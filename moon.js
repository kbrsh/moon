(function(window) {
    
    function Moon(opts) {
        var el = document.querySelector(opts.el),
            model = opts.model,
            children = el.childNodes
            
        // one way data binding
        for (m in model) {
            
        }   
    }
    
    
})(window);